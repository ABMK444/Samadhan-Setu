export const classificationService = {
  // FUTURE AI INTEGRATION: POST /api/challenges/classify. Manual choice only today.
  classifyChallenge: (_description: string, selectedCategory: string) =>
    selectedCategory,
};
